@model VenuesViewModel

@{
    ViewData["Title"] = "Venues";
}

<h1 class="display-4">@ViewData["Title"]</h1>
<div class="container mt-5">
    <div class="row">
        @foreach (var venue in Model.Venues)
        {
            <div class="col-md-6">
                @await Component.InvokeAsync("Venue", venue);
            </div>
        }
    </div>
</div>